{
  "name": "denoiser-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Denoiser Demo Application",
  "scripts": {
    "dev": "vite serve --port 8080 --open",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dest denoiser-demo"
  },
  "dependencies": {
    "@braveyao/denoiser": "file:../denoiser"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.3.2"
  }
}
